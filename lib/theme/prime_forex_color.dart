import 'package:flutter/material.dart';

class PrimeForexColors {
  // 🔹 Backgrounds
  static const Color bg = Color(0xFFF9FAFB);
  static const Color cardBg = Colors.white;
  static const Color transparent = Colors.transparent;

  static const Color primary = Color(0xFF6A1B9A); // Royal Purple
  static const Color primaryDark = Color(0xFF4A148C); // Darker Purple
  static const Color secondary = Color(0xFFCDAB5B); // Gold crown accent
  static Color shadow = primary.withOpacity(0.4);

  // 🔹 Buttons
  static const Color button = primary;
  static const Color buttonText = Colors.white;
  static const Color disabledButton = Colors.grey;

  // 🔹 Text
  static const Color text = Color(0xFF1A1A1A);
  static const Color textLight = Color(0xFF737373);
  static const Color textDark = Colors.black;
  static const Color headerText = Color(0xFF00A67E);

  // 🔹 Icons
  static const Color icon = Color(0xFF005F47);
  static const Color iconSecondary = Color(0xFF9CA3AF);

  // 🔹 Dividers / Borders
  static const Color divider = Color(0xFFD1D5DB);
  static const Color border = Color(0xFFE5E7EB);

  // 🔹 Gradients
  static const Color gradient1 = Color(0xFF6A1B9A);
  static const Color gradient2 = Color(0xFF4A148C);

  // 🔹 Alerts / States
  static const Color success = Colors.green;
  static const Color error = Color(0xFFD14343);
  static const Color warning = Colors.amber;
  static const Color info = Colors.blue;
}
